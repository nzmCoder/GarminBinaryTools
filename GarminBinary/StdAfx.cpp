// stdafx.cpp : source file that includes just the standard includes
//  GarminBinary.pch will be the pre-compiled header
//  stdafx.obj will contain the pre-compiled type information

#include "stdafx.h"

/*
    typedef struct timecaps_tag {
  UINT wPeriodMin;
  UINT wPeriodMax;
} TIMECAPS;

    void time_cap12()
    {
TIMECAPS tp;

timeGetDevCaps(&tp, sizeof(tp));

CString str;
str.Format("%d, %d", tp.wPeriodMin, tp.wPeriodMax);
AfxMessageBox(str);
    }
*/